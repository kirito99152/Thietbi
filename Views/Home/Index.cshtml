@{
    ViewData["Title"] = "Trang chủ";
}

<h1 class="title">Quản Lý Thiết Bị</h1>
<div class="container">
    <!-- Mục danh sách liên kết -->
    <div class="list">
        <a class="link" href="/ThietBi">Thiết Bị</a> <!-- Liên kết tới trang Thiết Bị -->
    </div>
    <div class="list">
        <a class="link" href="/LichSuCapPhat">Lịch Sử Cấp Phát</a> <!-- Liên kết tới trang Lịch Sử Cấp Phát -->
    </div>
    <div class="list">
        <a class="link" href="/LichSuSuaChua">Lịch Sử Sửa Chữa</a> <!-- Liên kết tới trang Lịch Sử Sửa Chữa -->
    </div>
    <div class="list">
        <a class="link" href="/QuanHeThietBi">Quan Hệ Thiết Bị</a> <!-- Liên kết tới trang Quan Hệ Thiết Bị -->
    </div>
</div>

<style>
    /* Reset và cài đặt chung */
    body {
        margin: 0; /* Loại bỏ khoảng cách mặc định */
        padding: 0; /* Loại bỏ padding mặc định */
        font-family: 'Arial', sans-serif; /* Cài đặt font chữ mặc định */
        background-color: lightgreen;/* Màu nền dịu mắt */
        color: #333; /* Màu chữ mặc định */
    }

    h1.title {
        text-align: center; /* Căn giữa tiêu đề */
        font-size: 36px; /* Kích thước chữ của tiêu đề */
        text-transform: uppercase; /* Chuyển tất cả chữ thành in hoa */
        color: #00796b; /* Màu chữ tiêu đề */
        margin: 30px 0; /* Khoảng cách trên và dưới tiêu đề */
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1); /* Tạo bóng đổ cho chữ */
    }

    .container {
        display: flex; /* Sử dụng Flexbox */
        flex-direction: column; /* Xếp các phần tử theo chiều dọc */
        align-items: center; /* Căn giữa các phần tử */
        gap: 20px; /* Khoảng cách giữa các mục menu */
        padding: 20px; /* Khoảng cách trong container */
    }

    /* Thiết kế danh sách */
    .list {
        width: 100%; /* Chiếm toàn bộ chiều rộng của phần tử chứa */
        max-width: 600px; /* Giới hạn chiều rộng tối đa là 600px */
        display: flex; /* Sử dụng Flexbox để căn chỉnh các liên kết */
        justify-content: center; /* Căn giữa các liên kết */
        background-color: #e0f7fa; /* Màu nền của mỗi mục menu */
        border-radius: 10px; /* Bo tròn các góc của mục menu */
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Tạo bóng đổ nhẹ cho mục menu */
        transition: transform 0.3s ease, box-shadow 0.3s ease; /* Thêm hiệu ứng chuyển động và bóng đổ */
    }

        .list:hover {
            transform: translateY(-5px); /* Nâng mục menu lên khi hover */
            box-shadow: 0 8px 12px rgba(0, 0, 0, 0.2); /* Tăng bóng đổ khi hover */
        }

    /* Thiết kế liên kết */
    .link {
        text-decoration: none; /* Loại bỏ gạch dưới của liên kết */
        color: #004d40; /* Màu chữ của liên kết */
        font-size: 20px; /* Kích thước chữ của liên kết */
        font-weight: bold; /* Chữ đậm cho liên kết */
        text-transform: uppercase; /* Chuyển tất cả chữ thành in hoa */
        padding: 15px 20px; /* Khoảng cách trong liên kết */
        display: block; /* Hiển thị liên kết dưới dạng khối */
        width: 100%; /* Chiếm toàn bộ chiều rộng của mục menu */
        text-align: center; /* Căn giữa chữ trong liên kết */
        transition: color 0.3s ease; /* Thêm hiệu ứng chuyển màu khi hover */
    }

        .link:hover {
            color: #00251a; /* Thay đổi màu chữ khi hover */
        }
</style>
